import React, { FC } from "react";
import FormHeader from "./FormHeader";
import FormBody from "./FormBody";

const Form: FC = () => {
  return (
    <>
      <FormHeader />
      <FormBody />
    </>
  );
};

export default Form;
